@model WorkSchedule
@{
	ViewData["Title"] = "Đặt lịch";
}
<link rel="stylesheet" href="~/css/WorkSchedule/Create.css"/>
<div class="container mt-4">
	<div class="row my-2">
		<div class="col text-primary"><h2>Thêm lịch mới</h2></div>
	</div>
	<form method="post">
		<input value="@ViewBag.phongkham.Name" disabled/>
		<input asp-for="PhongKhamId" value="@ViewBag.phongkham.RoomId" hidden />
		<input asp-for="DayOfWeek" value="@ViewBag.Thu" readonly />
		<label>Chọn bác sĩ</label>
		<select asp-for="DoctorId1" asp-items="@ViewBag.doctorlistca1">
			<option value="">--Chọn--</option>
		</select>
		<select asp-for="DoctorId2" asp-items="@ViewBag.doctorlistca2">
			<option value="">--Chọn--</option>
		</select>
		<select asp-for="DoctorId3" asp-items="@ViewBag.doctorlistca3">
			<option value="">--Chọn--</option>
		</select>
		<button type="submit" class="btn btn-primary">Đặt</button>
	</form>
</div>