@model IEnumerable<PhanMemWebQuanLiBenhVien.Models.MedicalRecord>
@{
    ViewData["Title"] = "Danh Sách Bệnh Án";
}
<link rel="stylesheet" href="~/css/MedicalVisit/Index.css" />
<div class="container mt-4">
    <div class="row my-2">
        <div class="col text-primary"><h2>Danh Sách Bệnh Án</h2></div>
        <div class="col col-2 text-end btn btn-primary text-center"><h5><i class="bi bi-person-plus-fill"></i> <a asp-action="AdminSideCreate" asp-controller="MedicalRecord" class="text-decoration-none text-white">Thêm</a></h5></div>
    </div>
    <table class="table table-striped table-bordered">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Tên Bệnh nhân</th>
                <th>Giới tính</th>
                <th>BHYT</th>
                <th>Địa chỉ</th>
                <th>Tiền sử bệnh án</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var record in Model)
            {
                <tr>
                    <td>@record.MedicalRecordId</td>
                    <td>@record.PatientName</td>
                    <td>@record.PatientGender</td>
                    <td>@record.BHYT</td>
                    <td>@record.Address</td>
                    <td>@record.TienSuBenhAn</td>
                    <td>
                        <a class="btn btn-primary" asp-controller="MedicalRecord" asp-action="Update" asp-route-id="@record.MedicalRecordId"><i class="bi bi-pencil-square"></i> Sửa</a>
                        <a asp-controller="MedicalRecord" asp-action="Detail" asp-route-id="@record.MedicalRecordId" class="btn btn-primary"><i class="bi bi-layout-text-sidebar-reverse"></i> Chi tiết</a>
                    </td>
                </tr>
            }
        </tbody> 
    </table>
</div>